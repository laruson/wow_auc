<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gmail.chernikovich.wow_auctionator">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".presenter.screen.auction.AuctionActivity">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity android:name=".presenter.screen.group.create.CreateGroupActivity" />
        <activity android:name=".presenter.screen.group.find.FindGroupActivity" />
        <activity android:name=".presenter.screen.item.ItemActivity" />
        <activity android:name=".presenter.screen.token.TokenActivity" />
    </application>

</manifest>
